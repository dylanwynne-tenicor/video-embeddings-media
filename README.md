# Requirements
- Python3.11
- See requirements.txt

# To run
- first time: pip install -r requirements.txt
- index videos first with: python main.py
- then run the streamlit server with: streamlit run app.py --server.headless true
